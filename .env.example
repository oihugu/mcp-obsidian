# ==============================================================================
# MCP Obsidian API Configuration
# ==============================================================================

# ------------------------------------------------------------------------------
# Obsidian API Configuration
# ------------------------------------------------------------------------------
# API key from Obsidian Local REST API plugin
OBSIDIAN_API_KEY=your-obsidian-api-key-here

# Obsidian API host (use localhost for local, or domain for remote)
OBSIDIAN_HOST=127.0.0.1
# OBSIDIAN_HOST=secondbrain.oihugudev.com.br

# Obsidian API port
OBSIDIAN_PORT=27124

# Protocol (http or https)
OBSIDIAN_PROTOCOL=http

# ------------------------------------------------------------------------------
# Authentication Configuration
# ------------------------------------------------------------------------------
# JWT Secret Key (CHANGE THIS IN PRODUCTION!)
JWT_SECRET_KEY=your-super-secret-jwt-key-please-change-this-in-production

# JWT token expiration in minutes (default: 1440 = 24 hours)
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# API Keys (comma-separated, for programmatic access)
API_KEYS=key1-change-this,key2-change-this,key3-change-this

# Admin password (CHANGE THIS IN PRODUCTION!)
ADMIN_PASSWORD=changeme-in-production

# ------------------------------------------------------------------------------
# Server Configuration
# ------------------------------------------------------------------------------
# Server host (0.0.0.0 to accept external connections)
SERVER_HOST=0.0.0.0

# Server port
SERVER_PORT=8000

# Enable auto-reload (development only)
SERVER_RELOAD=false

# Number of worker processes (production)
SERVER_WORKERS=1

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# ------------------------------------------------------------------------------
# Rate Limiting
# ------------------------------------------------------------------------------
# Maximum requests per minute per client
RATE_LIMIT_PER_MINUTE=60

# ------------------------------------------------------------------------------
# CORS Configuration
# ------------------------------------------------------------------------------
# Allowed CORS origins (comma-separated, use * for all)
CORS_ORIGINS=*
# CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# ------------------------------------------------------------------------------
# Security
# ------------------------------------------------------------------------------
# Trusted hosts (comma-separated, optional)
TRUSTED_HOSTS=
# TRUSTED_HOSTS=mcp-obsidian.yourdomain.com,localhost

# ------------------------------------------------------------------------------
# Cloudflare Tunnel (for deployment)
# ------------------------------------------------------------------------------
# Your tunnel UUID (get from: cloudflared tunnel create)
# CLOUDFLARE_TUNNEL_UUID=your-tunnel-uuid-here

# Your domain
# CLOUDFLARE_DOMAIN=mcp-obsidian.yourdomain.com

# ------------------------------------------------------------------------------
# Monitoring (Optional)
# ------------------------------------------------------------------------------
# Enable Prometheus metrics
# ENABLE_METRICS=true

# Metrics port
# METRICS_PORT=9090
